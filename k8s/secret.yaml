##############################################################################
# WARNING: Do NOT commit real credentials to source control.
# Use a secrets manager (Vault, AWS Secrets Manager, Azure Key Vault, etc.)
# and inject values at deploy time, or use a K8s External Secrets Operator.
#
# The values below are base64 placeholders — replace before applying:
#   echo -n 'jdbc:oracle:thin:@//oracle-host:1521/MYPDB' | base64
##############################################################################
apiVersion: v1
kind: Secret
metadata:
  name: batch-upload-db-secret
  namespace: batch
  labels:
    app: batch-upload
type: Opaque
data:
  # base64( jdbc:oracle:thin:@//oracle-host:1521/MYPDB )
  DB_URL: amRiYzpvcmFjbGU6dGhpbkAvL29yYWNsZS1ob3N0OjE1MjEvTVlQREI=
  # base64( batch_user )
  DB_USERNAME: YmF0Y2hfdXNlcg==
  # base64( your-strong-password )
  DB_PASSWORD: eW91ci1zdHJvbmctcGFzc3dvcmQ=
